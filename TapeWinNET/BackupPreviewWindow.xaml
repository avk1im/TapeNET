<Window x:Class="TapeWinNET.BackupPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TapeWinNET.ViewModels"
        xmlns:converters="clr-namespace:TapeWinNET.Converters"
        d:DataContext="{d:DesignInstance Type=vm:BackupPreviewViewModel}"
        mc:Ignorable="d"
        Title="Backup Preview"
        Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <converters:BooleanToVisibilityCollapsedConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="Delete" Command="{Binding RemoveSelectedCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding BackCommand}"/>
    </Window.InputBindings>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="{Binding TotalFileCountDisplay}" FontSize="14" FontWeight="SemiBold"/>
            <TextBlock Text=" - " FontSize="14"/>
            <TextBlock Text="{Binding SelectedTotalSizeDisplay}" FontSize="14"/>
        </StackPanel>

        <!-- File List -->
        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Files}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  SelectionMode="Extended">
            <ListView.View>
                <GridView>
                    <!-- Checkbox column -->
                    <GridViewColumn Width="30">
                        <GridViewColumn.Header>
                            <CheckBox IsChecked="{Binding DataContext.SelectAll, RelativeSource={RelativeSource AncestorType=ListView}}"
                                      ToolTip="Select/Deselect All"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- Icon column -->
                    <GridViewColumn Width="28">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Width="16" Height="16" Source="{Binding Icon}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" Width="200" 
                                    DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Header="Size" Width="80" 
                                    DisplayMemberBinding="{Binding SizeDisplay}"/>
                    <GridViewColumn Header="Modified" Width="140" 
                                    DisplayMemberBinding="{Binding LastModifiedDisplay}"/>
                    <GridViewColumn Header="Path" Width="350" 
                                    DisplayMemberBinding="{Binding Directory}"/>
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Remove Selected" Command="{Binding RemoveSelectedCommand}"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>

        <!-- Bottom Button Bar -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="{Binding SelectedFileCountDisplay}" 
                           VerticalAlignment="Center" Margin="0,0,12,0"/>
                <Button Content="Remove Selected" Command="{Binding RemoveSelectedCommand}" 
                        Width="110"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="← Back" Command="{Binding BackCommand}" 
                        Width="80" Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="Start Backup" Command="{Binding StartBackupCommand}" 
                        Width="100" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>