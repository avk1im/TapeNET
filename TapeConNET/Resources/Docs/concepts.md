# tapecon — Concepts

> Placeholder topic. Filled in by **Phase 8**.

`tapecon` is the command-line companion to **TapeWinNET**, sharing the same
underlying `TapeService` and `TapeLibNET` library. Run `tapecon --help` for
the verb tree, or `tapecon docs migration` for the 1.0 → 2.0 migration guide.
